<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ config.theme.extensions.title }}</title>
    <icon>{{ config.theme.extensions.base_url }}{{ config.theme.extensions.favicon_ico }}</icon>
    <link href="{{ config.theme.extensions.base_url }}/atom.xml" rel="self"/>
    <link href="{{ config.theme.extensions.base_url }}"/>
    <id>{{ config.theme.extensions.base_url }}/atom.xml</id>
    <updated>{{ context.updated_at | date(format="%Y-%m-%dT%H:%M:%SZ", timezone="UTC") }}</updated>
    <subtitle>{{ config.theme.extensions.description }}</subtitle>

    {% for article in context.items %}
    <entry>
        <id>{{ config.theme.extensions.base_url }}/articles/{{ article.article_id }}</id>
        <title>{{ article.title }}</title>
        <link href="{{ config.theme.extensions.base_url }}/articles/{{ article.article_id }}" rel="alternate" type="text/html"/>
        <author>
            <name>{{ config.theme.extensions.title }}</name>
        </author>
        <content type="html">
            {% if not article.need_password %}
            {{ article.render_content }}
            {% else %}
            {{ "🔒 文章内容需要解锁查看" }}
            {% endif %}
        </content>
        <published>{{ article.published_at | date(format="%Y-%m-%dT%H:%M:%SZ", timezone="UTC") }}</published>
        <updated>{{ article.updated_at | date(format="%Y-%m-%dT%H:%M:%SZ", timezone="UTC") }}</updated>
    </entry>
    {% endfor %}
</feed>